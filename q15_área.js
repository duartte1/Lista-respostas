const input = require('prompt-sync')()

// Entrada
const area = Number(input('Valor da area: '))

// Processamento
const base = 3
const altura = 5
const valor_area = base * altura /2

// Saida
console.log('O valor da area é', valor_area)