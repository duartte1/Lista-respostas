const input = require('prompt-sync')()

// Entrada
const area = Number(input('Valor do lado: '))

// Processamento
const lado = 5
const valor_area = lado * lado

// Saída
console.log('O valorda area é', valor_area)