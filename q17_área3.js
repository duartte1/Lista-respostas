const input = require('prompt-sync')()

// Entrada
const area = Number(input('Valor da base e altura: '))

// Processamento
const base = 7
const altura = 9
const valor_area = base * altura

// Saída
console.log('O valor da area é', valor_area)